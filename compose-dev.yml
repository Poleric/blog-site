services:
    web:
        build: .
        user: '${UID}:${GID}'
        volumes:
            - ./apps:/app/apps
        ports:
            - '8000:8000'
        env_file:
            - path: default.env
              required: true
            - path: .env
              required: true
        environment:
            - DJANGO_SETTINGS_MODULE=apps.blogsite.settings.dev
